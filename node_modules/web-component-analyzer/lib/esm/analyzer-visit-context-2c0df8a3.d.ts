import * as tsModule from "typescript";
import { Node, TypeChecker } from "typescript";
import { AnalyzerFlavor, ComponentFeatureCollection } from "./analyzer-flavor-2c0df8a3";
import { AnalyzerConfig } from "./analyzer-config-fd82e405";
/**
 * This context is used in the entire analyzer.
 * A new instance of this is created whenever the analyzer runs.
 */
interface AnalyzerVisitContext {
    checker: TypeChecker;
    ts: typeof tsModule;
    config: AnalyzerConfig;
    flavors: AnalyzerFlavor[];
    emitContinue?(): void;
    cache: {
        featureCollection: WeakMap<Node, ComponentFeatureCollection>;
        general: Map<unknown, unknown>;
    };
}
export { AnalyzerVisitContext };
//# sourceMappingURL=analyzer-visit-context-2c0df8a3.d.ts.map